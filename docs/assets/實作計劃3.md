實作建議計畫書：八面體攤平與 8 面等面積映射 (拓樸修正版)本計畫嚴格遵照 SDG4PDEOnSphere20260422a.pdf 的理論架構。我們將整個球面拓樸「攤平」至一個 $[-1, 1] \times [-1, 1]$ 的 2D 正方形域，並透過內接菱形將其精準劃分為 8 個特定的三角形區塊（$T_1 \dots T_8$）。透過此架構與正確的「拓樸縫合」，局部 Jacobian 將是完美的常數，RHS 算子將達到極致的簡化。階段一：網格攤平與正方形域劃分 (Mesh Flattening)我們將計算域定義在全局正方形 $\Omega = [-1, 1] \times [-1, 1]$ 上，座標為 $(X, Y)$。利用座標軸與**「內接菱形（$|X|+|Y|=1$）」**，將正方形劃分為 8 個基礎三角形：1. 北半球 (內接菱形內部)這 4 個三角形共用正方形的中心點 $(0,0)$（對應北極點 North Pole），其赤道為菱形的四個邊界：$T_1$: 第一象限，滿足 $X \ge 0, Y \ge 0$ 且 $X+Y \le 1$$T_3$: 第二象限，滿足 $X \le 0, Y \ge 0$ 且 $-X+Y \le 1$$T_5$: 第三象限，滿足 $X \le 0, Y \le 0$ 且 $-X-Y \le 1$$T_7$: 第四象限，滿足 $X \ge 0, Y \le 0$ 且 $X-Y \le 1$2. 南半球 (正方形四個角落)這 4 個三角形位於菱形外部。正方形的四個頂點 $(1,1), (-1,1), (-1,-1), (1,-1)$ 全都會映射到球面的同一個點（南極點 South Pole）：$T_2$: 第一象限，滿足 $X \ge 0, Y \ge 0$ 且 $X+Y \ge 1$$T_4$: 第二象限，滿足 $X \le 0, Y \ge 0$ 且 $-X+Y \ge 1$$T_6$: 第三象限，滿足 $X \le 0, Y \le 0$ 且 $-X-Y \ge 1$$T_8$: 第四象限，滿足 $X \ge 0, Y \le 0$ 且 $X-Y \ge 1$實作細節：直接在這 8 個基礎三角形內進行 2D 平面的均勻網格細分，子元素的 Jacobian ($J_{affine}$) 將為平凡常數。階段二：拓樸縫合與連接圖 (Topological Gluing)在計算 DG 面通量 (Face Flux) 時，必須認知到正方形 $[-1, 1]^2$ 的外圍邊界不是實體牆壁。必須建立全域的「拓樸縫合表 (Connectivity Map)」，手動將切開的球面縫合：右側邊界縫合 ($X=1$)：第一象限外側的 $T_2$ 右邊界，與第四象限外側的 $T_8$ 右邊界在物理球面上是相連的。上方邊界縫合 ($Y=1$)：第一象限的 $T_2$ 上邊界，與第二象限的 $T_4$ 上邊界相連。左側邊界縫合 ($X=-1$)：第二象限的 $T_4$ 左邊界，與第三象限的 $T_6$ 左邊界相連。下方邊界縫合 ($Y=-1$)：第三象限的 $T_6$ 下邊界，與第四象限的 $T_8$ 下邊界相連。實作要求：在網格生成模組中，必須透過座標比對，將上述這些原本在 2D 陣列中被視為「Domain Boundary」的邊界，互相設為對方的鄰居節點 (mapP)。階段三：反變速度的 8 種情境 (Analytical Contravariant Velocity)因為映射函數在 $T_1 \dots T_8$ 這八個區域的代數形式不同，其偏導數矩陣 $A$ 以及逆矩陣 $A^{-1}$ 有 8 套獨立的解析形式。實作要求： 必須實作一個具備 8 個分支 (Branches) 的反變速度轉換函數。在求解器初始化或每個時間步，根據子元素所屬的區域標籤計算速度：def compute_contravariant_velocity(u_phys, v_phys, lam, theta, face_id, R_sphere):
    """
    根據節點所屬的基礎三角形 (T1 ~ T8, face_id 1~8)，套用專屬的 A^-1 轉換矩陣。
    """
    # ... 根據 face_id 套用對應的 (a, b, c, d) 矩陣元素 ...
    # 例如 face_id == 1:
    #   u_tilde = (d * u_phys - b * v_phys) / (pi * R^2)
    #   ...
    return u_tilde, v_tilde
階段四：RHS 的極簡化與執行常數 Jacobian 擔保：根據 PDF，所有 $T_1 \dots T_8$ 映射的面積拉伸率皆為 $\sqrt{G} = \pi R^2$。捨棄 DGCL 修正：因為我們是在 $[-1, 1]^2$ 上進行完美的常數度量計算，完全不需要再計算數值散度。純淨的體積分：體積分算子嚴格退回極簡形式：$$\text{Volume Term} = D_r (\tilde{u} q) + D_s (\tilde{v} q)$$邊界通量 (Upwind Flux)：直接拿 8 種情境算出的反變速度 $(\tilde{u}, \tilde{v})$ 與參考三角形法向量 $(n_r, n_s)$ 內積，計算特徵波速 $V_n$ 並配合已縫合的拓樸表 (mapM, mapP) 進行通量交換。
